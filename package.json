{
	"name": "remote_previous",
	"version": "1.0.0",
	"description": "A simple plugin to allow remote contol users to move backwards in the queue without the remote 'hanging' from too many presses, too rapidly.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Redlawd",
	"license": "ISC",
	"volumio_info": {
		"prettyName": "remote_previous",
		"icon": "fa-lightbulb-o",
		"plugin_type": "user_interface"
	},
	"dependencies": {
		"fs-extra": "^0.28.0",
		"kew": "^0.7.0",
		"socket.io-client": "^1.7.4",
		"v-conf": "^1.4.0"
	}
}
