'use strict';

const AlbumArtHandler = require(__dirname + '/albumart');
const ServerPoller = require(__dirname + '/poller');

module.exports = {
    AlbumArtHandler: AlbumArtHandler,
    ServerPoller: ServerPoller
};