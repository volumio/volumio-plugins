'use strict';

const BrowseController = require(__dirname + '/browse');
const SearchController = require(__dirname + '/search');
const PlayController = require(__dirname + '/play');

module.exports = {
    BrowseController: BrowseController,
    SearchController: SearchController,
    PlayController: PlayController
};