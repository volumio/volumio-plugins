'use strict';

const workflow = require(__dirname + '/workflow');
const upload = require(__dirname + '/upload');
const install = require(__dirname + '/install');

module.exports = {
    workflow,
    upload,
    install
}