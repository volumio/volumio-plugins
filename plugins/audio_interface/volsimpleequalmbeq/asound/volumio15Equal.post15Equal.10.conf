#empty
#balbuze's config file

pcm.volumio15Equal {
    type plug
    slave.pcm "volEqual"
  #  slave.format "FLOAT"
}

pcm.volEqual {
    type ladspa
    slave.pcm "post15Equal"
    path "/usr/lib/ladspa"
    plugins [ 
		{
		id 1197
		label "mbeq"
		input {
			controls [ ${coef} ]
			}
    		}
 ]
}