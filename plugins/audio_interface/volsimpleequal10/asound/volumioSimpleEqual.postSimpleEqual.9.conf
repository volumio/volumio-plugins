
# Avoid using the default config location as it varies between users

pcm.volumioSimpleEqual {
    type lfloat
    slave.pcm "volSimpleEqual"
    slave.format "FLOAT"
}

pcm.volSimpleEqual {
    type equal

   	slave.pcm "postSimpleEqualConvert";


}

pcm.postSimpleEqualConvert {
    type lfloat
    slave.pcm "postSimpleEqual"
    slave.format "S24_LE"
}

ctl.volSimpleEqual {
	type equal
 
}
